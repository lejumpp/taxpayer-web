# Loaded by `pnpm dev` (mode: development). Overrides .env, overridden by .env.local.
VITE_API_URL=http://localhost:5000

# Paddle sandbox client-side token (publishable — safe to commit).
VITE_PADDLE_CLIENT_TOKEN=test_3829de97aee41a597954e983ea2
VITE_PADDLE_ENV=sandbox
