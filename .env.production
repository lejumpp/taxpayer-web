# Loaded by `pnpm build` (mode: production). Overrides .env.
# TODO: fill in real production values before deploying.
VITE_API_URL=https://api.example.com

# Paddle live client-side token (publishable — safe to commit).
VITE_PADDLE_CLIENT_TOKEN=
VITE_PADDLE_ENV=production
